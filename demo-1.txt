Git Commands

git config --global user.name "aishafemi71"
git config --global user.email "aishafemi71@gmail.com"

check both

>git init-view>hidden folder
>git add .  /- add demo.txt
>git remote add origin "https://github.com/aishafemi71/Demo.git"-differes with folders
git commit -m "testing"-refers to what is performed in the file or what it is about.
>git push -u origin master

when making changes to the existing file commands needed are add,commit and push

>git add .
git commit -m "testing"
>git push

CLONE
>git clone "https://github.com/rohan-hari/Postman-Clone-React.git"

>git status
>git branch branchname
>git checkout branchname--branchname can be anything you want it to be represented
